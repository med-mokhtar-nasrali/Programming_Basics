// This will count the numbers of likes
var likeCount = 0;
// this will increase the like by the amount written
function increaseLikes() {
    // this will increase the likes by only 1 like
    likeCount = likeCount + 1;
}
